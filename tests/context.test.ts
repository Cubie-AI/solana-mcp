describe("Context", () => {
  it("should throw an error if connection is not provided", () => {
    // import { Context } from "../src/context";
    // expect(() => {
    //   new Context({
    //     connection: undefined,
    //   });
    // }).toThrow("Connection is required");
  });

  it("should throw an error if connection is invalid", () => {
    // import { Context } from "../src/context";
    // expect(() => {
    //   new Context({
    //     connection: "invalid",
    //   });
    // }).toThrow("Invalid connection");
  });

  it("should create a context with a valid connection", () => {
    // import { Context } from "../src/context";
    // const context = new Context({
    //   connection: "https://api.mainnet-beta.solana.com",
    // });
    // expect(context).toBeDefined();
  });

  it("should throw an error if context is not valid", () => {
    // import { validateContext } from "../src/utils";
    // expect(() => {
    //   validateContext(undefined);
    // }).toThrow("Invalid context");
  });
});
